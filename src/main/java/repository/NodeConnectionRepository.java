package repository;

import entity.NodeConnection;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NodeConnectionRepository extends JpaRepository<NodeConnection, Long> {
}
